source_dir: files
targets:
  kitty.conf:
    dir: $HOME/.config/kitty
  ssh.conf:
    dir: $HOME/.ssh
    filename: config
  vimrc:
    dir: $HOME
    filename: .vimrc
  zshrc:
    dir: $HOME
    filename: .zshrc

